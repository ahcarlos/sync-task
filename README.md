# Sincronización fork con repo original

### Requisitos
```gem install rake```

### Uso
Si no está creado el remoto ```upstream```
  * ```rake sync <url_repo>```

Si está creado el remoto
  * ```rake sync```